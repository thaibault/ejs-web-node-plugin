<%- configuration.name %>
=========================

Tests:
------

<%- Tools.stringCompressStyleValue('border  1 px solid  red') %>

Configurations:
---------------

<%_ for (var key in configuration) { _%>
    - "<%- key %>" -> <%- typeof configuration[key] === 'string' ? configuration[key] : Tools.represent(configuration[key]) %>
<%_ } _%>

<%_
    const list = []
    for (let index = 0; index < 10; index++)
        list.push(index)
_%>
<%- list.join(', ') %>

data = <%- Tools.convertCircularObjectToJSON(typeof mockupData === 'undefined' ? {} : mockupData) %>;

<%_ let includedVariable = 1 _%>

<%- include('dummyInclude.txt', {includedVariable: 2}) %>

<%_ const name = 'Include' _%>

<%- include(`dummy${name}.txt`, {includedVariable}) %>
