<%- _.configuration.name %>
=========================

Tests:
------

<%- _.string.compressStyleValue('border  1 px solid  red') %>

Configurations:
---------------

<%_ for (var key in _.configuration) { _%>
    - "<%- key %>" -> <%- typeof _.configuration[key] === 'string' ? _.configuration[key] : _.object.represent(_.configuration[key]) %>
<%_ } _%>

<%_
    const list = []
    for (let index = 0; index < 10; index++)
        list.push(index)
_%>
<%- list.join(', ') %>

data = <%- _.object.convertCircularObjectToJSON(typeof _.mockupData === 'undefined' ? {} : _.mockupData) %>;

<%_ let includedVariable = 1 _%>

<%- _.include('dummyInclude.txt', {includedVariable: 2}) %>

<%_ const name = 'Include' _%>

<%- _.include(`dummy${name}.txt`, {includedVariable}) %>
